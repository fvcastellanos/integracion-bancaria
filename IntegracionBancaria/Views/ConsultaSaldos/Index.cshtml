@model IntegracionBancaria.Model.Views.ConsultaSaldosViewModel

@using (Html.BeginForm("Index", "ConsultaSaldos"))
{
    <div class="form-group">
        @Html.LabelFor(model => model.BancoId, "Banco")
        @Html.DropDownListFor(model => model.BancoId, new SelectList(Model.Bancos, "Id", "Nombre"), "Seleccionar Banco", new { @class = "form-control", @id = "banco" })
    </div>
}

<table id="resumen-cuentas" class="table table-striped">
    <thead>
        <tr>
            <th>Nombre Cuenta</th>
            <th>Moneda</th>
            <th>Saldo Disponible</th>
            <th>Saldo en Reserva</th>
            <th>Saldo Flotante</th>
            <th>Saldo Total</th>
        </tr>
    </thead>
    <tbody>
    </tbody>
</table>

@section Scripts
{    <script>
        (function() {
            $("#banco").change(function() {
                var bancoId = $(this).val();
                var $cuentasTable = $("#resumen-cuentas");
                if (bancoId && bancoId !== "") {
                    $cuentasTable.dataTable().fnDestroy();
                    $cuentasTable.DataTable({
                        ajax: {
                            url: "/api/Mock/Usuarios/1/Bancos/" + bancoId + "/Cuentas/",
                            dataSrc: ""
                        },
                        columns: [
                            {
                                data: "nombreCuenta"
                            },
                            {
                                data: "moneda"
                            },
                            {
                                data: "saldoDisponible"
                            },
                            {
                                data: "saldoEnReserva"
                            },
                            {
                                data: "saldoFlotante"
                            },
                            {
                                data: "saldoTotal"
                            }
                        ]
                    });      
                } else {
                    $cuentasTable.DataTable()
                        .clear()
                        .draw();
                }
            });

        })();
    </script>
}
